export interface ConfigInterface {
    max_temp: number | number[],
    min_temp: number | number[],
    max_hum: number | number[],
    min_hum: number | number[],
    server_notification_enambled: boolean
}